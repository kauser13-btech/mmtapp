import Image from "next/image";
const Sticker = ({ extraClass, isCheckout = false }) => {
  return (
    // <div className={"flex flex-col gap-4 " + extraClass}>
    //   {isCheckout && <Image className="w-auto" src={"/images/mmt-sticker-2.png"} alt="Sticker" width={100} height={100} />}
    //   {!isCheckout && <Image className="w-auto" src={"/images/mmt-sticker-1.png"} alt="Sticker" width={100} height={100} />}
    // </div>
    <></>
  );
};

export default Sticker;
