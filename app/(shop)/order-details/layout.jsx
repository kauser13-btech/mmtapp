export const metadata = {
  title: 'Order Details Page',
  description: 'Order Details Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
