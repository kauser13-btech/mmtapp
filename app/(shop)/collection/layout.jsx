export const metadata = {
  title: 'Collection Page',
  description: 'Collection Page desc',
}

const layout = ({ children }) => {

  return (
    < >
      {children}
    </>
  );
};

export default layout;
