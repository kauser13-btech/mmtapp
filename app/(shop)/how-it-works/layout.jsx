export const metadata = {
  title: 'How it work Page',
  description: 'How it work Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
