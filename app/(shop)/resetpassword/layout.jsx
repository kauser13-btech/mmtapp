export const metadata = {
  title: 'Reset Password Page',
  description: 'Reset Password Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
