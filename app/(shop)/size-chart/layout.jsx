export const metadata = {
  title: 'Size Chart Page',
  description: 'Size Chart Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
