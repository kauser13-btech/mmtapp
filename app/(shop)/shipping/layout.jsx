export const metadata = {
  title: 'Shipping Page',
  description: 'Shipping Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
