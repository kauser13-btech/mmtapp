export const metadata = {
  title: 'Discount Page',
  description: 'Discount Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
