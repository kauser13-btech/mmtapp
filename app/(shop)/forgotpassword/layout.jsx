export const metadata = {
  title: 'Forgot Password Page',
  description: 'Forgot Password Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
