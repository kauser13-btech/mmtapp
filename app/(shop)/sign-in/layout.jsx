export const metadata = {
  title: 'Sign in Page',
  description: 'Sign in Page desc',
}
const layout = ({ children }) => {

  return (
    <>
      {children}
    </>
  );
};

export default layout;
